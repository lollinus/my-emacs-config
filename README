My emacs configuration files. -*- mode: org mode: auto-fill -*-
#+STARTUP: showall

* Setup

** Windows

** U*x

   Depending on Linux distribution follow build instructions in
   =./deployment/emacs-config-*.sh= and =./deployment/build.emacs.org=.
   List of 3rd party libs required to build can be found in =./deployment/emacs-pkgs*.txt=.
