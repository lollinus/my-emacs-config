;; rc-linum.el
;; (add-to-list 'load-path
;;              (if (string-equal "21" (substring emacs-version 0 2))
;;                  (concat my-site-lisp-directory "linum/emacs21")
;;                (concat my-site-lisp-directory "linum/emacs22")
;;                )
;;              )

;; ;; line numbering with linum
;; (require 'linum)
;(global-linum-mode t)
