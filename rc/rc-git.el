;;; rc-git.el ---

;;(ensure-package-installed 'git)
(ensure-package-installed 'git-blame)
;; (add-to-list 'load-path
;;              (concat my-site-lisp-directory "git"))
;;----------------------------------------------------------------------
;; git mode
;;----------------------------------------------------------------------
;; (require 'git)
;; (require 'git-blame)

;;; rc-git.el ends here
